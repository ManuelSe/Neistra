// Test-only extension: enables real browser zoom through chrome.tabs.setZoom.
// It reads/writes no application or server data and is never shipped with Neistra.
chrome.runtime.onInstalled.addListener(() => {});
