/** Product identity changes; the portable archive contract does not. */
export function ArchiveFormatHelp() {
  return (
    <p className="archive-format-help">
      Neistra Archives use the existing <code>.molweave.zip</code> format.
      Existing MolWeave archives remain supported.
    </p>
  );
}
